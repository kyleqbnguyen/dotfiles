tmux send-keys -t "$TS_SESSION:1" "cd ~/vault && clear" C-m
tmux new-window -t "$TS_SESSION:" -c ~/vault/concepts -n vim 'nvim; exec $SHELL'
tmux new-window -t "$TS_SESSION:" -c ~/vault/concepts -n agent
tmux new-window -t "$TS_SESSION:" -c "$TS_PATH" -n sioyek "pdf=\$(find . -maxdepth 1 -type f -name '*.pdf' -print -quit); if [[ -n \"\$pdf\" ]]; then sioyek \"\$pdf\"; fi; exec \$SHELL"
tmux select-window -t "$TS_SESSION:vim"
